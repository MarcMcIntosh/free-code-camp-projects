import { DEFAULT_STATE as barChart } from '../../projects/bar-chart';
import { DEFAULT_STATE as calculator } from '../../projects/calculator';
import { DEFAULT_STATE as dataGlobe } from '../../projects/data-globe';
import { DEFAULT_STATE as dungeonCrawler } from '../../projects/dungeon-crawler';
import { DEFAULT_STATE as forceDirected } from '../../projects/force-directed';
import { DEFAULT_STATE as gameOfLife } from '../../projects/game-of-life';
import { DEFAULT_STATE as heatMap } from '../../projects/heat-map';
import { DEFAULT_STATE as leaderBoard } from '../../projects/leader-board';
import { DEFAULT_STATE as localWeather } from '../../projects/local-weather';
import { DEFAULT_STATE as markdownPreviewer } from '../../projects/markdown-previewer';
import { DEFAULT_STATE as pomodoroClock } from '../../projects/pomodoro';
import { DEFAULT_STATE as portfolio } from '../../projects/portfolio';
import { DEFAULT_STATE as quoteMachine } from '../../projects/quote-machine';
import { DEFAULT_STATE as recipeBox } from '../../projects/recipe-box';
import { DEFAULT_STATE as scatterPlotGraph } from '../../projects/scatterplot-graph';
import { DEFAULT_STATE as simon } from '../../projects/simon';
import { DEFAULT_STATE as ticTacToe } from '../../projects/tic-tac-toe';
import { DEFAULT_STATE as tributePage } from '../../projects/tribute-page';
import { DEFAULT_STATE as twitchTv } from '../../projects/twitch-tv';
import { DEFAULT_STATE as wikipediaViewer } from '../../projects/wikipedia-viewer';
import { DEFAULT_STATE as fileSize } from '../../projects/file-size';
import { DEFAULT_STATE as imageSearch } from '../../projects/image-search';
import { DEFAULT_STATE as urlShortener } from '../../projects/url-shortener';
import { DEFAULT_STATE as headerParser } from '../../projects/header-parser';
import { DEFAULT_STATE as timestamp } from '../../projects/timestamp';
import { DEFAULT_STATE as session } from './session';

const DEFAULT_STATE = {
  barChart,
  calculator,
  dataGlobe,
  dungeonCrawler,
  fileSize,
  forceDirected,
  gameOfLife,
  headerParser,
  heatMap,
  imageSearch,
  leaderBoard,
  localWeather,
  markdownPreviewer,
  pomodoroClock,
  portfolio,
  quoteMachine,
  recipeBox,
  scatterPlotGraph,
  session,
  simon,
  ticTacToe,
  timestamp,
  tributePage,
  twitchTv,
  urlShortener,
  wikipediaViewer,
};

export default DEFAULT_STATE;
