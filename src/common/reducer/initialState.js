import { DEFAULT_STATE as barChart } from '../../projects/bar-chart/reducer';
import { DEFAULT_STATE as calculator } from '../../projects/calculator/reducer';
import { DEFAULT_STATE as dataGlobe } from '../../projects/data-globe/reducer';
import { DEFAULT_STATE as dungeonCrawler } from '../../projects/dungeon-crawler/reducer';
import { DEFAULT_STATE as forceDirected } from '../../projects/force-directed/reducer';
import { DEFAULT_STATE as gameOfLife } from '../../projects/game-of-life/reducer';
import { DEFAULT_STATE as heatMap } from '../../projects/heat-map/reducer';
import { DEFAULT_STATE as leaderBoard } from '../../projects/leader-board/reducer';
import { DEFAULT_STATE as localWeather } from '../../projects/local-weather/reducer';
import { DEFAULT_STATE as markdownPreviewer } from '../../projects/markdown-previewer/reducer';
import { DEFAULT_STATE as pomodoroClock } from '../../projects/pomodoro/reducer';
import { DEFAULT_STATE as quoteMachine } from '../../projects/quote-machine/reducer';
import { DEFAULT_STATE as recipeBox } from '../../projects/recipe-box/reducer';
import { DEFAULT_STATE as scatterPlotGraph } from '../../projects/scatterplot-graph/reducer';
import { DEFAULT_STATE as simon } from '../../projects/simon/reducer';
import { DEFAULT_STATE as ticTacToe } from '../../projects/tic-tac-toe/reducer';
import { DEFAULT_STATE as tributePage } from '../../projects/tribute-page/reducer';
import { DEFAULT_STATE as twitchTv } from '../../projects/twitch-tv/reducer';
import { DEFAULT_STATE as wikipediaViewer } from '../../projects/wikipedia-viewer/reducer';

const DEFAULT_STATE = {
  barChart,
  calculator,
  dataGlobe,
  dungeonCrawler,
  forceDirected,
  gameOfLife,
  heatMap,
  leaderBoard,
  localWeather,
  markdownPreviewer,
  pomodoroClock,
  quoteMachine,
  recipeBox,
  scatterPlotGraph,
  simon,
  ticTacToe,
  tributePage,
  twitchTv,
  wikipediaViewer,
};

export default DEFAULT_STATE;
