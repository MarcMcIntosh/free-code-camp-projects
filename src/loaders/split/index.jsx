import React from 'react';
import classnames from 'classnames/bind';
import styles from './split.scss';

const cx = classnames.bind(styles);
const Split = () => (<div className={cx('loader')} />);

export default Split;
