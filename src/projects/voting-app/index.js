/*
* An evaluation of the options nis that is would be best imnplmented as a sub routerConfiguration using
* universal then and then the configuration.
* a hoc might work to
* like this https://reacttraining.com/react-router/web/example/route-config
* <Route
    path={route.path}
    render={props => (
      // pass the sub-routes down to keep nesting
      <route.component {...props} routes={route.routes} />
    )}
  />
*
}
*/
