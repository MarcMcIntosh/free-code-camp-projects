import React, { Component } from 'react';
import Modal from 'react-modal';
const Recipe = recipe => (
  <Modal>
   {/* Some veiw componet with edit button */}
   {/* Editor with cancle and close button */}
  </Modal>
);

export default Recipe;
