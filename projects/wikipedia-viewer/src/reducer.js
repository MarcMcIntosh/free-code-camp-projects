
const DEFAULT_STATE = {
  isFetching: false,
  results: [],
  term: '',
  error: '',
};

function reducer(state = DEFAULT_STATE, action) {
  switch (action.type) {
    default: return state;
  }
}

export default reducer;
